A TodoApp collection API.

I used the REST Client Vscode extension to handle the HTTP request (task.rest).

npm start to start the server.

MongoDB automatically create an id, so each field has an id.

